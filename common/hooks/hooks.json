{
  "description": "Bootstrapper: auto-load the cross-project knowledge index at session start (every session, any repo), copy the current project's skills from the canonical ia repo into the clone's .claude/skills (injecting that project's knowledge index too), and remind to capture reusable knowledge -- global and project-scoped -- before context compaction.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/skills/knowledge-base/scripts/session_start.py\"",
            "timeout": 10
          },
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/scripts/link_project_skills.py\"",
            "timeout": 10
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/skills/knowledge-base/scripts/remind_capture.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
